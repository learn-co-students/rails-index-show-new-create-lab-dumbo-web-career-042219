<ul>
  <% @coupons.each do |linked_coupon| %>
    <li>
      Store: <%= linked_coupon.store %><br>
      <%= link_to "#{linked_coupon.coupon_code}", coupon_path(linked_coupon) %><br>
    </li>
  <% end %>
</ul>
